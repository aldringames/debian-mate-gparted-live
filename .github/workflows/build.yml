name: Build

on: push

jobs:
  buid:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main
      - name: Build
        run: ./build.sh
      - name: Release
        uses: softprops/action-gh-release@master
        with:
          name: Debian MATE GParted Live ${{ env.DATE }}
          tag_name: ${{ env.DATE }}
          files: |
            debian-mate-gparted-live-${{ env.DATE }}.iso
            debian-mate-gparted-live-${{ env.DATE }}.iso.sha256
            debian-mate-gparted-live-${{ env.DATE }}.iso.md5
